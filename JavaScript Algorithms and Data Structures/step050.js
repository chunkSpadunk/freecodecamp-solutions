// here we are defining the function
function functionWithArgs(param1, param2) {
  console.log(param1 + param2);
}

// now here we will al the function
functionWithArgs(1, 2);
functionWithArgs(7, 9);
