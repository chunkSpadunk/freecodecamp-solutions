function isLess(a, b) {
    // only change the code below this line
    return a <= b;
}

console.log(isLess(10, 15));
console.log(isLess(15, 10));
// we should fix this code by eliminating the use of if and else and writing an expression to still solve the way it is intended
