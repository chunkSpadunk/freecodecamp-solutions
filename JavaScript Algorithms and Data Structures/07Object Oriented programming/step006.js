function Dog() {
    this.name = "Rupert";
    this.color = "brown";
    this.numLegs = 4;
}
// this inside the constructor always references to the object being created
// only change the code below this line
// create a new instance of Dog
let hound = new Dog();
