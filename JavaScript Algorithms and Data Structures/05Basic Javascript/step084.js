const myDog = {
    // Only change code below this line
    // we are going to write the properties of objects now
    name: "this would contain the first name and the last name",
    legs: 5,
    tails: 6,
    friends: [1, 2, 3, 4],
    // Only change code above this line
};
