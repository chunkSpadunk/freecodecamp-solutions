function randomWholeNum() {
    // Only change code below this line
    // to make the random number between a two numbers we have to do it the following the way
    var result = 0;
    result = Math.floor(Math.random() * 10);
    return result;
}

randomWholeNum();
